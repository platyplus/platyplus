import './redirection.module.less'

/* eslint-disable-next-line */
export interface RedirectionProps {}

export const Redirection = (props: RedirectionProps) => {
  return <div></div>
}

export default Redirection
