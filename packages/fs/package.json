{
  "name": "@platyplus/fs",
  "private": false,
  "version": "0.0.5",
  "description": "A wrapper of several filesystem utils meant to supplement the standard fs package",
  "author": "Pierre-Louis Mercereau <24897252+plmercereau@users.noreply.github.com>",
  "homepage": "https://platy.dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/platyplus/platyplus.git",
    "directory": "packages/fs"
  },
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prepublish": "yarn run build",
    "build": "yarn run clean && tsc -p tsconfig.build.json",
    "test": "echo \"Error: run tests from root\" && exit 1",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.1",
    "@types/merge-deep": "^3.0.0",
    "rimraf": "^3.0.2",
    "tsconfig-paths": "^3.9.0"
  },
  "dependencies": {
    "fs-extra": "^9.0.1",
    "glob": "^7.1.6",
    "merge-deep": "^3.0.2",
    "yaml": "^1.10.0"
  }
}
