apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
  namespace: default
data:
  {{- if index .Values.ingress.hosts 0 }}
  serverUrl: "https://{{ index .Values.ingress.hosts 0 }}"
  {{- else }}
  serverUrl: ""
  {{- end }}
